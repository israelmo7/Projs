#!/usr/bin/env python3
"""
Legacy serial handler — superseded by the FastAPI backend.

Run the full-stack dashboard instead:
    ./dev.sh
    # or
    cd backend && PYTHONPATH=..:. uvicorn backend.main:app --port 8000
"""

print(__doc__)
